{
  "name": "glob-failure-parent",
  "version": "1.0.0",
  "description": "Parent for glob-failure reproduce, exists as separate, parent project to install `glob` without complicating the test case",
  "main": "index.js",
  "scripts": {
    "start": "cd fail-case && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mitch",
  "license": "ISC",
  "devDependencies": {
    "glob": "^7.1.2"
  }
}
